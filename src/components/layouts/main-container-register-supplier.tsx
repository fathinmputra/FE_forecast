const MainContainerRegisterSupplier = ({ children }: { children: React.ReactNode }) => {
  return (
    <div
      className="dark:text-white-dark min-h-screen text-black"
    >
      {children}
    </div>
  );
};

export default MainContainerRegisterSupplier;